import { BudgetItem } from "@/types/api";

interface BudgetItemProps {
  item?: BudgetItem;
  isLoading?: boolean;
}

export default function BudgetItemSummary({
  item,
  isLoading,
}: BudgetItemProps) {
  return <></>;
}
